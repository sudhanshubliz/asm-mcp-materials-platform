version: '3'

services:
  mcp-server:
    build: .
    ports:
      - "8000:8000"
    environment:
      - REDIS_HOST=redis
      - VECTOR_DB=http://qdrant:6333

  redis:
    image: redis:7
    ports:
      - "6379:6379"

  qdrant:
    image: qdrant/qdrant:latest
    ports:
      - "6333:6333"
